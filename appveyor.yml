environment:
  nodejs_version: "14"

install:
  - ps: Install-Product node $env:nodejs_version
  - npm install

version: 1.0.{build}
build_script:
- ps: >-
    npm install -g pkg-fetch@2.5.8

    npm install -g pkg

    cd Node.js
    
    npm install

    cd ..

    cd build